# quality - rules (apply by default)
- A feature is not launch-ready until it passes a QA run on a stable staging env; a flaky env is a release risk, not a QA problem to work around. (confirmed 5x; promoted 2026-08)
