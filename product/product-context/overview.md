# Acme Insights - product overview (demo)
Acme Insights is a B2B analytics platform: connect your data, build dashboards, and get alerted when metrics move. The wedge for Q4 is **smart-alerts** (threshold + anomaly alerts), with a natural-language query layer (**AQL**) as the next bet.
